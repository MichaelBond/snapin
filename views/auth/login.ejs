<!DOCTYPE html>
<html>
<head>
  <meta name="apple-mobile-web-app-capable" content="yes"/>
	<title>Node Authentication</title>
  <link type="text/css" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.1/css/font-awesome.min.css" rel="stylesheet" ></link>
  <link type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet" ></link>
	<style>
		body 		{ padding-top:80px; }
</style>
</head>
<body>
<div class="container">

<div class="col-sm-6 col-sm-offset-3">

	<h1><span class="fa fa-sign-in"></span> Login</h1>
	<% console.log("MESSAGE:",message, message.length, message.error) %>
	<p>Version 1</p>
	<!-- show any messages that come back with authentication -->
	<% if (message && message.length && message[0].error) { %>
	  <div class="alert alert-danger">
	    <%= message[0].error %>
	  </div>
	<% } %>

	<!-- LOGIN FORM -->
	<form action="/auth/login" method="post">
		<div class="form-group">
			<label>Email</label>
			<input type="text" class="form-control" name="username" value="<%= (message.length && message[0].formData) ? message[0].formData.email : '' %>" required/>
		</div>
		<div class="form-group">
			<label>Password</label>
			<input type="password" class="form-control" name="password" value="<%= (message.length && message[0].formData) ? message[0].formData.password : '' %>" required/>
		</div>

		<button type="submit" class="btn btn-warning btn-lg">Login</button>
	</form>

	<hr />

	<p>Need an account? <a href="javascript:window.parent.switchToSignup()">Signup</a></p>
	<p>Or go <a href="javascript:window.parent.closePopups()">home</a>.</p>

</div>

</div>
</body>
</html>
