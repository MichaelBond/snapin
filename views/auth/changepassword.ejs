<!DOCTYPE html>
<html lang="en">
<head>
  <meta name="apple-mobile-web-app-capable" content="yes"/>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Node Authentication - Change Password</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.1/css/font-awesome.min.css">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
  <style>
    body {
      padding-top: 80px;
      background-color: #1a1a1a;  /* Dark background */
      color: #f0f0f0;              /* Light text */
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
    }
    .container {
      max-width: 600px;
      background-color: #333; /* Darker background for container */
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
    }
    h1 {
      font-size: 2.5em;
      font-weight: 300;
      color: #ffffff;  /* White header text */
      margin-bottom: 20px;
    }
    .form-group label {
      font-weight: 600;
      color: #f0f0f0;  /* Light label text */
    }
    .form-control {
      background-color: #2a2a2a;  /* Darker background for form inputs */
      color: #f0f0f0;             /* Light text in inputs */
      border: 1px solid #555;     /* Border for inputs */
      margin-bottom: 15px;
    }
    .form-control:focus {
      background-color: #3a3a3a;  /* Slightly lighter on focus */
      color: #ffffff;
    }
    .btn-warning {
      background-color: #f0ad4e;
      border-color: #eea236;
      color: #1a1a1a;  /* Dark button text */
    }
    .btn-warning:hover {
      background-color: #ec971f;
      border-color: #d58512;
    }
    .alert-danger {
      font-size: 1.2em;
      background-color: #ff4d4d;  /* Red background for error alert */
      color: #fff;                /* White text */
    }
    hr {
      margin-top: 30px;
      margin-bottom: 30px;
      border-color: #555;         /* Light divider */
    }
    .text-center a {
      color: #f0ad4e;             /* Light gold for links */
    }
    .text-center a:hover {
      color: #ec971f;             /* Hover effect for links */
    }
  </style>
</head>
<body>
<div class="container">

  <div class="col-sm-12">

    <!-- Header -->
    <h1><span class="fa fa-lock"></span> Change Password</h1>

    <p class="text-muted">Version 1</p>

    <!-- Display any error messages -->
    <% if (message && message.length && message[0].error) { %>
      <div class="alert alert-danger">
        <%= message[0].error %>
      </div>
    <% } %>

    <!-- Change Password Form -->
    <form action="/auth/changepassword" method="post">
      <!-- Current Password input -->
      <div class="form-group">
        <label>Current Password</label>
        <input type="password" class="form-control" name="currentPassword" required/>
      </div>

      <!-- New Password input -->
      <div class="form-group">
        <label>New Password</label>
        <input type="password" class="form-control" name="newPassword" required/>
      </div>

      <!-- Confirm New Password input -->
      <div class="form-group">
        <label>Confirm New Password</label>
        <input type="password" class="form-control" name="confirmNewPassword" required/>
      </div>

      <!-- Submit Button -->
      <button type="submit" class="btn btn-warning btn-lg btn-block">Change Password</button>
    </form>

    <hr />

    <!-- Footer Links -->
    <p class="text-center">
      <a href="/auth/login">Login</a>
    </p>

  </div>

</div>
</body>
</html>
