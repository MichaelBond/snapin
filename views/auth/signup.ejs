<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Signup | Node Authentication</title>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.1/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css">
    <style>
        body {
            padding-top: 40px;
            background-color: #1a1a1a;  /* Dark background */
            color: #f0f0f0;              /* Light text */
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }
        .container {
            max-width: 800px;
            background-color: #333; /* Darker background for container */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.5);
        }
        h1 {
            font-size: 2.5em;
            font-weight: 300;
            color: #ffffff;  /* White header text */
            margin-bottom: 20px;
        }
        .form-group label {
            font-weight: 600;
            color: #f0f0f0; /* Light text for labels */
        }
        .btn-warning {
            background-color: #f0ad4e;
            border-color: #eea236;
            color: #1a1a1a;  /* Dark text on the button */
        }
        .btn-warning:hover {
            background-color: #ec971f;
            border-color: #d58512;
        }
        .form-control {
            background-color: #2a2a2a;  /* Darker background for form fields */
            color: #f0f0f0;             /* Light text for inputs */
            border: 1px solid #555;     /* Border for inputs */
            margin-bottom: 15px;
        }
        .form-control:focus {
            background-color: #3a3a3a;  /* Slightly lighter when focused */
            color: #ffffff;
        }
        hr {
            margin-top: 30px;
            margin-bottom: 30px;
            border-color: #555;         /* Light color for the divider */
        }
        .alert-danger {
            font-size: 1.2em;
            background-color: #ff4d4d;  /* Red background for error alert */
            color: #fff;                /* White text for the error */
        }
        .signup-header {
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 20px;
        }
        .signup-header img {
            height: 50px;
            margin-right: 15px;
        }
        .text-muted {
            color: #aaa !important;  /* Light gray for muted text */
        }
        .text-center a {
            color: #f0ad4e; /* Light gold for links */
        }
        .text-center a:hover {
            color: #ec971f;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="col-sm-12 col-md-8 col-md-offset-2">
            
            <!-- Header Section -->
            <div class="signup-header">
                <img src="https://s3.amazonaws.com/smartweb-pro.com/cdn/smart/images/svg/snapin_white.svg" alt="Logo" />
                <h1>Signup</h1>
            </div>

            <p class="text-muted">Version 6</p>

            <% if (message && message.length && message[0].error) { %>
                <div class="alert alert-danger">
                    <%= message[0].error %>
                </div>
            <% } %>

            <!-- Signup Form -->
            <form action="/auth/signup" method="post">
                
                <!-- Contact Information -->
                <div class="form-group">
                    <label>Email <i>(Required)</i></label>
                    <input type="email" class="form-control" name="email" value="<%= (message.length && message[0].formData) ? message[0].formData.email : '' %>" required>
                </div>
                <div class="form-group">
                    <label>Confirm Email <i>(Required)</i></label>
                    <input type="email" class="form-control" name="email2" value="<%= (message.length && message[0].formData) ? message[0].formData.email2 : '' %>" required>
                </div>

                <!-- Password Information -->
                <div class="form-group">
                    <label>Password <i>(Required)</i></label>
                    <input type="password" class="form-control" name="password" required>
                </div>
                <div class="form-group">
                    <label>Confirm Password <i>(Required)</i></label>
                    <input type="password" class="form-control" name="password2" required>
                </div>

                <!-- Additional Information -->
                <div class="row">
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label>Company Name</label>
                            <input type="text" class="form-control" name="companyname" value="<%= (message.length && message[0].formData) ? message[0].formData.companyname : '' %>">
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label>Job Title</label>
                            <input type="text" class="form-control" name="title" value="<%= (message.length && message[0].formData) ? message[0].formData.title : '' %>">
                        </div>
                    </div>
                </div>

                <!-- Personal Information -->
                <div class="row">
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label>First Name</label>
                            <input type="text" class="form-control" name="firstname" value="<%= (message.length && message[0].formData) ? message[0].formData.firstname : '' %>">
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label>Last Name</label>
                            <input type="text" class="form-control" name="lastname" value="<%= (message.length && message[0].formData) ? message[0].formData.lastname : '' %>">
                        </div>
                    </div>
                </div>

                <!-- Address Information -->
                <div class="row">
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label>Phone Number</label>
                            <input type="tel" class="form-control" name="phonenumber" value="<%= (message.length && message[0].formData) ? message[0].formData.phonenumber : '' %>">
                        </div>
                    </div>
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label>Street Address</label>
                            <input type="text" class="form-control" name="address" value="<%= (message.length && message[0].formData) ? message[0].formData.address : '' %>">
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-sm-6">
                        <div class="form-group">
                            <label>City</label>
                            <input type="text" class="form-control" name="city" value="<%= (message.length && message[0].formData) ? message[0].formData.city : '' %>">
                        </div>
                    </div>
                    <div class="col-sm-3">
                        <div class="form-group">
                            <label>State</label>
                            <input type="text" class="form-control" name="state" value="<%= (message.length && message[0].formData) ? message[0].formData.state : '' %>">
                        </div>
                    </div>
                    <div class="col-sm-3">
                        <div class="form-group">
                            <label>Zip Code</label>
                            <input type="text" class="form-control" name="zip" value="<%= (message.length && message[0].formData) ? message[0].formData.zip : '' %>">
                        </div>
                    </div>
                </div>

                <!-- Submit Button -->
                <button type="submit" class="btn btn-warning btn-lg btn-block">Sign Up</button>

            </form>

            <hr>

            <!-- Footer Links -->
            <p class="text-center">
                Already have an account? <a href="javascript:window.parent.switchToLogin()">Login</a> <br />
                Or go <a href="javascript:window.parent.closePopups()">home</a>.
            </p>
        </div>
    </div>
</body>
</html>
